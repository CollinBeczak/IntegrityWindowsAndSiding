import { Box } from "@mui/material";
import React from "react";

const Showcase = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
        marginY: 20,
      }}
    >
      Our showcase page is not up yet.
    </Box>
  );
};

export default Showcase;
