import { Box } from "@mui/material";
import React from "react";

const Siding = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
        marginY: 20,
      }}
    >
      This page is not up and ready yet, but we do specialize in replacing Siding, so please feel
      free to contact us directly or with the form below.
    </Box>
  );
};

export default Siding;
