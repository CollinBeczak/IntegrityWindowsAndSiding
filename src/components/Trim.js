import { Box } from "@mui/material";
import React from "react";

const Trim = () => {
  return (
    <Box
      sx={{
        textAlign: "center",
        marginY: 20,
      }}
    >
      This page is not up and ready yet, but we do specialize in replacing Trim, so please feel free
      to contact us directly or with the form below.
    </Box>
  );
};

export default Trim;
